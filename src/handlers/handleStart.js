import sendMessage from "../sendMessage.js";

export default async function handleStart(chatId) {
  return sendMessage(
    chatId,
`🌱 *欢迎来到 DailyGround*

每天和我随便聊聊天，我会悄悄帮你：
- 提取你的日常素材
- 建立“故事库 / 素材库”
- 生成你可以直接发布的内容提案（小红书/抖音/Threads）

你不用会写，也不用想选题。
像朋友一样和我聊就好。

想说说今天的一个小瞬间吗？🙂
`
  );
}
